# Enterprise Sampling

Enterprise sampling surfaces map to `truthound.profiler.enterprise_sampling`.

Use these pages for large-dataset profiling and suite generation workflows where
progressive or multi-stage sampling is required.
