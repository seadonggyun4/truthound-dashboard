# Deployment

Deployment requires:

- a Python environment with `truthound-dashboard`
- frontend assets built from `frontend/`
- docs built with `mkdocs build --strict`

Dashboard docs are mirrored into the main Truthound docs portal under the
`dashboard/` prefix.
