# Realtime

Realtime coverage in the dashboard tracks the public `truthound.realtime`
namespace. The dashboard documents and surfaces only upstream streaming and
incremental validation concepts that are available in Truthound 3.0.
