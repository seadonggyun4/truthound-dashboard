# Privacy

Privacy workflows use the Truthound root APIs:

- `truthound.scan` for PII detection
- `truthound.mask` for masking or redaction

The dashboard keeps the existing UI patterns for scan and mask review while
removing mock-only privacy demonstrations.
