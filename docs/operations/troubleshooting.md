# Troubleshooting

## Common checks

- confirm `truthound>=3.0.0`
- confirm frontend build and type-check pass
- confirm no deleted routes remain referenced in the UI shell
- confirm docs build with `mkdocs build --strict`
- confirm checkpoint views use `validation_run` or `validation_view`
