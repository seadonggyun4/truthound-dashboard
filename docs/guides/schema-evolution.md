# Schema Evolution

Schema evolution in the 3.0 dashboard is a comparison workflow, not a separate
schema watcher product.

Use schema evolution screens to compare learned schema or profile snapshots and
review structural changes between versions.
