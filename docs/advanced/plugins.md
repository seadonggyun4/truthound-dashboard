# Plugins

The dashboard plugin experience is aligned to the public `truthound.plugins`
surface, especially plugin discovery, registry, metadata, and documentation
extraction.

Dashboard-only browser editor/tester experiences are intentionally removed from
the 3.0 surface.
