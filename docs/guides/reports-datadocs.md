# Reports and Data Docs

Reporting surfaces are aligned to:

- `truthound.reporters`
- `truthound.datadocs`

The dashboard focuses on generating, browsing, and persisting report artifacts
instead of maintaining dashboard-specific report semantics.
