# Rule Suggestions

Rule suggestion flows use Truthound profiling and rule-learning features:

- `truthound.profiler.generate_suite`
- `truthound.ml.rule_learning`

The dashboard keeps the existing selection and export UI patterns while
targeting the Truthound 3.0 rule-generation surface.
