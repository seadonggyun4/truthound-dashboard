# Drift

Drift analysis is aligned to Truthound 3.0 namespace imports:

```python
from truthound.drift import compare
```

The dashboard exposes comparison-oriented drift workflows and avoids the
previous standalone dashboard drift-monitor product surface.
