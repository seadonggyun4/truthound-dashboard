# Sources

The dashboard source workflow is aligned to Truthound 3.0 source handling.

- File sources are passed as paths.
- Database and platform sources are converted to Truthound datasource objects.
- Schema learning is driven by `truthound.learn`.
- Profiling is driven by `truthound.profile`.

Use source detail pages to learn schemas, run validations, and inspect the most
recent profile or validation run.
